  "version": 2,
  "buildCommand": "rm -rf node_modules/.vite && rm -rf dist && npm run build",
  "outputDirectory": "dist",
  "devCommand": "npm run dev",
  "installCommand": "rm -rf node_modules && npm install",
  "framework": null,
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
